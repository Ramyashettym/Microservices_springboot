server:
  port: 8081
  
  
spring:
  datasource:
    url: jdbc:mysql://localhost:3006/EmployeeDB
    username: root
    password: Root@123456
    driver-class-name: com.mysql.cj.jdbc.Driver
    
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    
    database-platform: org.hibernate.dialect.MySQLDialect
    
  application:
    name: EMPLOYEE-SERVICE